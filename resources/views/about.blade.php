<x-app-layout>
    <div class="grid gap-4">
        <div class="p-4 bg-white rounded-lg shadow-xs">
            {{ __('About the system.') }}
        </div>

        <div class="p-4 bg-white rounded-lg shadow-xs">
            {{ __('About the developers.') }}
        </div>

        <div class="p-4 bg-white rounded-lg shadow-xs">
            {{ __('Instruction on how to use the system.') }}
        </div>
    </div>
</x-app-layout>
